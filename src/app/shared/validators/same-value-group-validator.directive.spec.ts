import { SameValueGroupValidatorDirective } from './same-value-group-validator.directive';

describe('SameValueGroupValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SameValueGroupValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
